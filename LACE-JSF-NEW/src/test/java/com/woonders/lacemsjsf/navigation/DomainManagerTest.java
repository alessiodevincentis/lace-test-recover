package com.woonders.lacemsjsf.navigation;

import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

import org.junit.Before;
import org.junit.Test;

/**
 * Created by emanuele on 24/11/16.
 */
public class DomainManagerTest {

	private DomainManager domainManager;

	@Before
	public void before() {
		domainManager = new DomainManager();
	}

	@Test
	public void isFromMainDomainTest() {
//                assertTrue(domainManager.isFromMainDomain("www.lacems.com"));
//                assertTrue(domainManager.isFromMainDomain("lacems.com"));
//                assertTrue(domainManager.isFromMainDomain("dev.lacems.com"));
//                assertTrue(domainManager.isFromMainDomain("staging.lacems.com"));

//		assertFalse(domainManager.isFromMainDomain("tenant1.lacems.com"));
//		assertFalse(domainManager.isFromMainDomain("tenant1.dev.lacems.com"));
//		assertFalse(domainManager.isFromMainDomain("tenant1.staging.lacems.com"));
	}
}
